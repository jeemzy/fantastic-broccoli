# Инструкция: HTML-письма PROAUTO в Thunderbird

В папке есть два готовых письма:

- `email-01-zapytanie-o-zgode.html` - первое письмо, только запрос согласия на получение informacji handlowej.
- `email-02-oferta-serwisowa.html` - второе письмо, коммерческое предложение после получения согласия.

Рекомендуемые темы писем:

- `Pytanie o zgodę na przesłanie informacji handlowej`
- `Oferta serwisowa dla floty pojazdów firmowych | proautopoland.eu`

## Важное юридическое предупреждение

База адресов указана как публичные cold emails. Первое письмо сделано максимально осторожно: оно не содержит предложения, скидок, прайса или списка услуг, а только спрашивает согласие на отправку informacji handlowej.

Это снижает риск, но не гарантирует юридическую безопасность рассылки. В Польше с 10 ноября 2024 действует Prawo komunikacji elektronicznej, Dz.U. 2024 poz. 1221, art. 398. Перед массовой отправкой лучше подтвердить текст и процесс у юриста: https://isap.sejm.gov.pl/isap.nsf/DocDetails.xsp?id=WDU20240001221

Практическое правило:

1. На публичные cold emails сначала отправляйте только `email-01-zapytanie-o-zgode.html`.
2. Не отправляйте второе письмо, пока адресат явно не ответил согласием.
3. Если ответа нет, не отправляйте оферту.
4. Если адресат просит не писать, удалите его из базы.

## Что изменить перед отправкой

Во втором письме замените плейсхолдер `[DATA]` на реальный срок акции, например `31.05.2026`.

В первом письме плейсхолдеров нет. В обоих письмах уже указаны:

- сервисный адрес: `ul. Bukowska 86, 62-070 Dopiewo`;
- сайт: `https://proautopoland.eu/`;
- подпись: `Zespół PROAUTO`;
- юридические реквизиты в нижнем фирменном блоке.

Нижний фирменный блок снова встроен прямо в HTML через `data:image/png;base64`. Поэтому письмо можно отправлять как один HTML-код без отдельного PNG-вложения.

## Как вставить HTML в Thunderbird

Локальный Thunderbird найден по пути:

`G:\apps2\thunderbird\thunderbird.exe`

Самый надежный ручной способ:

1. Откройте Thunderbird.
2. Нажмите `Write` / `Создать`.
3. Укажите получателя. Для теста сначала отправьте письмо самому себе: `zlosimo061@gmail.com`.
4. Укажите тему письма из списка выше.
5. В окне написания письма откройте меню `Insert` -> `HTML`.
   Если меню не видно, нажмите `Alt`.
6. Откройте нужный `.html` файл в редакторе кода или Блокноте.
7. Скопируйте весь HTML-код.
8. Вставьте код в окно `Insert HTML` и нажмите `Insert`.
9. Проверьте внешний вид письма в окне Thunderbird.
10. Отправьте тест самому себе.

Не используйте способ `открыть HTML в браузере -> Ctrl+A -> Ctrl+C -> вставить в Thunderbird` для финальной отправки. При таком копировании Thunderbird/Gmail может потерять часть HTML-структуры и стилей: исчезают цветные боксы, скругления, тени, а картинка может растянуть письмо на всю ширину.

Если `Insert HTML` работает некорректно, лучше не отправлять письмо через ручное копирование из браузера. В таком случае используйте систему рассылки, которая умеет импортировать HTML-код письма, или попросите меня подготовить вариант для конкретного инструмента отправки.

## Проверка перед массовой отправкой

Перед отправкой адресатам проверьте:

- письмо отправляется в формате HTML, а не plain text;
- польские символы отображаются корректно: `ą`, `ę`, `ł`, `ó`, `ś`, `ż`, `ź`;
- ссылки показываются как `proautopoland.eu` и открывают `https://proautopoland.eu/`;
- нижний фирменный блок виден;
- во втором письме заменен `[DATA]`;
- первое письмо не содержит оферты или перечня услуг;
- второе письмо отправляется только тем, кто дал согласие.

## Если картинка пропала

В HTML-файлы встроена картинка через `data:image/png;base64`, поэтому файлы открываются сами по себе. Но часть почтовых клиентов может удалять или блокировать такие картинки.

Если после тестовой отправки картинка не видна:

1. В Thunderbird поставьте курсор в конец письма.
2. Выберите `Insert` -> `Image`.
3. Укажите файл `proauto-info-email.png`.
4. Включите вариант прикрепления картинки к письму, если Thunderbird предложит такой выбор.
5. Отправьте тест еще раз.

Для больших рассылок более надежный вариант - использовать систему рассылки, которая вставляет изображения как inline/CID или хранит их на HTTPS-сервере.

## Рекомендуемый порядок работы

1. Отправьте `email-01-zapytanie-o-zgode.html` тестом на `zlosimo061@gmail.com`.
2. Откройте тестовое письмо в Gmail и Thunderbird.
3. Если вид письма корректный, отправляйте первое письмо адресатам.
4. Собирайте только явные ответы-согласия.
5. Перед отправкой второго письма замените `[DATA]`.
6. Отправьте `email-02-oferta-serwisowa.html` только тем адресатам, которые дали согласие.
